# Historical blobs containing credential patterns that have been REVIEWED and whose
# credentials were ROTATED. Format:  <blob-sha>   # reason
#
# This file records "we know about this one and it is dead" — never "hide this one".
# A blob belongs here only AFTER rotation at the provider, because removing a value from
# git does not un-leak it: anyone who cloned or forked before the rewrite still has it,
# and the value stays valid until the provider invalidates it.
#
# 🔴 Reasons must NOT restate the credential. Naming the blob is what the sha is for —
# quoting the value back turns this ledger into one more copy of the leak. The pre-commit
# hook blocks that, correctly, and it caught the first draft of this very file.
#
# scan-history.sh fails if an entry here no longer matches anything, so a rewritten or
# garbage-collected history cannot leave stale excuses behind.

2a027fec078752fcbe4d05fe818c42e7ac36c769  # tools/trustpilot/config.py — hardcoded proxy host:port:user:pass pairs. Committed in 1055694, redacted in e73c17f; the literal values are enumerated in the per-repo rules file (untracked) so re-pasting them is blocked. ROTATION IS ASSUMED, NOT VERIFIED: the claim comes from this repo's own rules-file header, and confirming it would mean authenticating against the proxy provider. If that rotation never happened, these are live credentials sitting in public git history — verify before trusting this line.
